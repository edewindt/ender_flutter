import "package:flutter/material.dart";

class Drawer_s extends StatelessWidget {
  const Drawer_s({super.key});

  @override
  Widget build(BuildContext context) {
    return Drawer(
      child: Text('Drawer initiated'),
    );
  }
}
